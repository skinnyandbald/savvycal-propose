# managed-by: skinnyandbald/github-defaults - do not edit, changes will be overwritten
name: Claude Code Review

on:
  pull_request:
    types: [opened, ready_for_review]
    branches: [main, master, develop]

jobs:
  claude-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
    env:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Run Claude Code Review
        if: ${{ env.CLAUDE_CODE_OAUTH_TOKEN != '' }}
        id: claude-review
        uses: anthropics/claude-code-action@d331729f3bc18428d4f810ed9b7a26d6176c803e # v1.0.87
        with:
          claude_code_oauth_token: ${{ env.CLAUDE_CODE_OAUTH_TOKEN }}
          # Linear's GitHub app opens/toggles PRs it syncs; without this the action
          # refuses with "Workflow initiated by non-human actor: linear-code".
          allowed_bots: 'linear-code'
          plugin_marketplaces: 'https://github.com/anthropics/claude-code.git'
          plugins: 'code-review@claude-code-plugins'
          prompt: '/code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}'
